# Copy to .env (gitignored) and fill in a real key for local development.
# The backend reads this from the environment — never commit a real value here.
# Alternative for local dev: `dotnet user-secrets set OPENAI_API_KEY <value>` from backend/.
OPENAI_API_KEY=
